{
  "name": "@woven-deep/engine",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run || { [ -n \"$CI\" ] && vitest run; }",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@woven-deep/content": "0.0.0",
    "rot-js": "2.2.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "fast-check": "4.8.0"
  }
}
